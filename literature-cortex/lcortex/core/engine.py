# Legacy engine placeholder — redirect to engine_legacy for backward compatibility.
# Use lcortex.monitor.run_controller.RunController.run_full_pipeline() instead.
from lcortex.core.engine_legacy import run_pipeline
